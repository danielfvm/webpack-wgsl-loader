// This file is an example wgsl file that is being imported into triangle.wgsl

const color = vec4<f32>(1.0, 0.0, 0.0, 1.0);
const vertices = array<vec2<f32>, 3>(
	vec2<f32>(-0.5, -0.5),
	vec2<f32>(0.5, -0.5),
	vec2<f32>(0.0, 0.5),
);
